# دليل المساهمة في Building Forge

مرحباً بك في مشروع Building Forge! نحن نرحب بمساهماتك ونقدر وقتك وجهدك.

## 🚀 البدء السريع

### المتطلبات الأساسية

- Node.js 18+ 
- npm 9+
- Git

### إعداد بيئة التطوير

```bash
# 1. استنساخ المشروع
git clone https://github.com/your-org/building-forge.git
cd building-forge

# 2. تثبيت التبعيات
npm install

# 3. إعداد بيئة التطوير (يتم تلقائياً بعد npm install)
npm run setup

# 4. تشغيل بيئة التطوير
npm run dev
```

## 🧪 الاختبار

### تشغيل الاختبارات

```bash
# جميع الاختبارات
npm run test:all

# اختبارات الوحدة فقط
npm run test:unit

# اختبارات الخصائص (Property-based) فقط
npm run test:property

# اختبارات التغطية
npm run test:coverage

# فحص الكود
npm run lint:check
```

### كتابة الاختبارات

#### اختبارات الوحدة
```typescript
import { describe, it, expect } from 'vitest'
import { myFunction } from '../myModule'

describe('MyModule', () => {
  it('should work correctly', () => {
    expect(myFunction()).toBe(expectedResult)
  })
})
```

#### اختبارات الخصائص
```typescript
import * as fc from 'fast-check'
import { runPropertyTest, vector3Arbitrary } from '../shared/property-test-utils'

describe('Property-based Tests', () => {
  it('should maintain invariants', () => {
    runPropertyTest(
      'Vector3 operations',
      vector3Arbitrary(),
      (vector) => {
        // التحقق من الخصائص
        expect(isValidVector3(vector)).toBe(true)
      }
    )
  })
})
```

## 📝 معايير الكود

### TypeScript

- استخدم `strict: true` في جميع الأوقات
- اكتب types صريحة للدوال العامة
- استخدم interfaces بدلاً من types عند الإمكان
- اتبع naming conventions:
  - PascalCase للـ classes و interfaces
  - camelCase للـ functions و variables
  - UPPER_SNAKE_CASE للـ constants

### React

- استخدم functional components مع hooks
- اكتب PropTypes أو TypeScript interfaces للـ props
- استخدم React.memo للتحسين عند الحاجة
- اتبع hooks rules

### Three.js

- استخدم React Three Fiber بدلاً من Three.js المباشر
- اكتب cleanup functions للـ geometries و materials
- استخدم useFrame بحذر لتجنب مشاكل الأداء

## 🏗️ بنية المشروع

```
src/
├── main/           # العملية الرئيسية لـ Electron
│   ├── main.ts     # نقطة الدخول الرئيسية
│   ├── preload.ts  # سكريبت preload للأمان
│   └── services/   # خدمات العملية الرئيسية
├── renderer/       # واجهة المستخدم (React)
│   ├── components/ # مكونات React
│   ├── hooks/      # Custom hooks
│   ├── services/   # خدمات العرض
│   ├── store/      # إدارة الحالة (Zustand)
│   └── utils/      # دوال مساعدة
└── shared/         # الكود المشترك
    ├── types.ts    # الأنواع المشتركة
    ├── constants.ts # الثوابت
    └── utils/      # دوال مساعدة مشتركة
```

## 🔄 سير العمل

### 1. إنشاء فرع جديد

```bash
git checkout -b feature/your-feature-name
# أو
git checkout -b fix/your-bug-fix
```

### 2. تطوير الميزة

- اكتب الكود مع اتباع المعايير
- اكتب الاختبارات (وحدة + خصائص)
- تأكد من نجاح جميع الاختبارات
- اكتب التوثيق إذا لزم الأمر

### 3. التحقق من الجودة

```bash
# فحص الكود
npm run lint:check

# تشغيل الاختبارات
npm run test:all

# فحص التغطية
npm run test:coverage
```

### 4. إرسال Pull Request

- اكتب عنوان واضح ووصف مفصل
- اربط بـ issue إذا كان موجود
- تأكد من نجاح CI/CD
- انتظر المراجعة

## 🐛 الإبلاغ عن الأخطاء

### قبل الإبلاغ

- تأكد من أن الخطأ لم يتم الإبلاغ عنه مسبقاً
- جرب إعادة إنتاج الخطأ
- اجمع معلومات النظام

### معلومات مطلوبة

- وصف واضح للخطأ
- خطوات إعادة الإنتاج
- السلوك المتوقع مقابل الفعلي
- لقطات شاشة إذا أمكن
- معلومات النظام (OS, Node.js version, etc.)

## ✨ اقتراح ميزات جديدة

### قبل الاقتراح

- تأكد من أن الميزة لم تُقترح مسبقاً
- فكر في التأثير على الأداء والتعقيد
- اعتبر البدائل الموجودة

### معلومات مطلوبة

- وصف واضح للميزة
- حالات الاستخدام
- أمثلة أو mockups إذا أمكن
- تأثير على الأداء والتعقيد

## 📋 قائمة المراجعة للـ Pull Request

- [ ] الكود يتبع معايير المشروع
- [ ] جميع الاختبارات تنجح
- [ ] التغطية لا تقل عن 80%
- [ ] لا توجد تحذيرات ESLint
- [ ] لا توجد أخطاء TypeScript
- [ ] التوثيق محدث إذا لزم الأمر
- [ ] الـ commit messages واضحة
- [ ] لا توجد ملفات غير ضرورية

## 🎯 أولويات التطوير

### عالية الأولوية
- أدوات البناء الأساسية (جدران، أرضيات)
- نظام المواد PBR
- التصدير للألعاب

### متوسطة الأولوية
- مكتبة الأصول
- أدوات القياس
- تحسينات الأداء

### منخفضة الأولوية
- ميزات UI متقدمة
- تكاملات خارجية
- أدوات إضافية

## 🤝 قواعد السلوك

- كن محترماً ومهذباً
- اقبل النقد البناء
- ساعد الآخرين
- اتبع معايير المجتمع

## 📞 التواصل

- GitHub Issues: للأخطاء والميزات
- GitHub Discussions: للأسئلة العامة
- Email: للمسائل الحساسة

---

شكراً لمساهمتك في Building Forge! 🏗️✨